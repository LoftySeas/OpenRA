# OpenRA 简体中文本地化实施计划

> 文档状态：可执行交接稿  
> 基准日期：2026-08-23  
> 目标分支：从最新 `OpenRA/OpenRA:bleed` 创建独立维护分支  
> 目标语言：简体中文（建议统一使用区域代码 `zh-CN`；目录命名是否使用 `zh` 由语言框架设计决定）

## 1. 背景与当前结论

OpenRA 已使用 Fluent 管理英文界面、规则、单位说明和战役文本，但当前上游 `bleed` 尚未正式提供可选择的中文语言包：

- `OpenRA.Game/Manifest.cs` 中 `FluentCulture` 当前固定为 `en`，并带有“应由用户选择翻译包”的 TODO。
- `OpenRA.Game/FluentProvider.cs` 当前直接加载清单中的英文 `FluentMessages`。
- 各官方 Mod 已把大量可翻译文本迁移到 `.ftl` 文件。
- 仓库已有 Fluent 语法及引用检查：
  - `OpenRA.Mods.Common/Lint/CheckFluentSyntax.cs`
  - `OpenRA.Mods.Common/Lint/CheckFluentReferences.cs`
- Windows 下可用 `./make.ps1 test` 对 TS、D2K、CNC、RA 运行 MiniYAML 和 Fluent lint；`./make.ps1 tests` 运行单元测试。

已有上游贡献：

- 中文 PR：[OpenRA/OpenRA#22531](https://github.com/OpenRA/OpenRA/pull/22531)，作者 Seaear。
- 截至本计划基准日期，该 PR 仍为 Open，尚未进入官方 `bleed`。
- PR 声称覆盖约 180 个文件、7300 行，包含公共界面、CNC、RA、D2K、TS、战役文本和 CJK 字体。
- PR 明确说明翻译由 LLM 辅助生成，专有名词、单位名称、语气和格式仍需人工复核。
- 该 PR 的提交历史包含俄语 PR [OpenRA/OpenRA#22517](https://github.com/OpenRA/OpenRA/pull/22517) 的两个提交，随后才是中文提交：
  - `4911cf2`：语言选择框架（来自俄语 PR）
  - `7dd4b49`：俄语翻译（来自俄语 PR）
  - `44f26e5`：中文翻译
  - `96c5ebf`：中文换行和格式修正

因此，**不得把 #22531 的完整分支未经拆分地作为长期汉化主线**。它应被视为：

1. 多语言实现原型；
2. 中文初始语料；
3. 待人工审校和工程验证的输入。

## 2. 总体目标

建立一个可运行、可验证、可持续跟随上游 `bleed` 更新，并有条件拆分回馈上游的简体中文版本。

最终成果应满足：

- 用户能在设置中选择简体中文，重启后稳定生效。
- 英文资源始终作为完整回退；缺少中文 key 时不得显示 key 名或导致崩溃。
- 中文资源不夹带俄语翻译或与中文无关的功能修改。
- 常用界面、单位、建筑、规则及官方战役具有一致且可理解的中文文本。
- Fluent 变量、属性、复数/选择表达式与英文源完全兼容。
- 中文字体覆盖实际使用字符，并且许可证允许随 GPL-3.0 项目分发。
- 每次同步上游后，可以自动识别新增、删除和发生结构变化的英文 key。
- 工程改动与翻译内容可以分别审查、分别提交、分别向上游贡献。

## 3. 非目标

第一阶段不承诺：

- 中文语音或重新配音；
- 一次性把所有第三方 Mod 和社区地图翻译成中文；
- 修改单位平衡、游戏规则或战役逻辑；
- 为了容纳中文而大规模重做全部 UI；
- 无人工复核地宣称“完整高质量汉化”；
- 将俄语、本地化框架和中文内容压成一个不可拆分的大提交。

## 4. 推荐的分支与提交结构

建议在个人 Fork 中维护以下分支：

| 分支 | 用途 | 是否长期维护 |
| --- | --- | --- |
| `bleed` | 只跟踪 `OpenRA/OpenRA:bleed` | 是 |
| `archive/pr-22531` | 保存 PR #22531 原始状态，仅作溯源和比较 | 否，冻结 |
| `feature/localization-framework` | 通用语言选择、资源覆盖和回退机制 | 是 |
| `feature/zh-cn-content` | 仅中文 Fluent 文件、术语和内容校订 | 是 |
| `feature/zh-cn-fonts` | 字体及字体配置；若足够小可并入框架分支 | 视情况 |
| `zh-cn` | 面向测试和发布的集成分支 | 是 |

推荐提交粒度：

1. `Add selectable Fluent language infrastructure`
2. `Add licensed CJK font support`
3. `Import initial zh-CN Fluent resources from PR #22531`
4. `Add zh-CN translation validation tooling`
5. `Define OpenRA zh-CN terminology glossary`
6. 按模块分别校订，例如 `Review common UI zh-CN translations`

不要把 180 个文件的导入、框架重构、字体替换和人工校订揉在同一个提交中。

## 5. 实施阶段

### 阶段 0：建立可追溯基线

任务：

1. 记录当前 `bleed` 的 commit SHA。
2. 读取 #22531 最新 head SHA、文件列表、提交列表、检查结果和讨论。
3. 保存原始 PR 分支或 patch，不修改其历史。
4. 建立来源记录，注明中文提交作者 Seaear、原提交 SHA、PR 链接和许可证来源。
5. 检查工作树，保护用户已有改动；不要覆盖或清理无关文件。

验收：

- 能从文档和 Git 历史追溯每一批导入文本的来源。
- 集成分支中不存在来源不明的二进制字体或翻译文件。

### 阶段 1：独立移植多语言框架

以 `4911cf2` 为实现参考，而不是无条件照搬。先比较它与当前 `bleed` 的架构差异，再只移植必要部分。

框架至少应处理：

1. 可用语言的声明和发现。
2. 用户语言设置的持久化。
3. 设置界面的语言下拉框。
4. 启动时选定 Fluent culture。
5. 英文基础资源优先完整加载，中文资源随后覆盖相同 key。
6. 中文缺失 key 时安全回退英文。
7. 地图级 Fluent 文本与 Mod 级文本的优先级不被破坏。
8. 无效或已删除语言配置安全回退到英文。
9. CNC、RA、D2K、TS 和 content Mod 的行为一致。
10. 专用服务器、工具程序和 lint 流程不依赖图形设置界面。

必须回答并记录的设计问题：

- 内部代码使用 `zh-CN`、`zh` 还是两者映射？
- 目录使用 `fluent/zh-CN/` 还是 `fluent/zh/`？
- 用户配置存放在哪里，旧配置如何迁移？
- 地图自带翻译是否允许覆盖官方 UI key？当前安全边界不能被削弱。
- lint 如何同时检查英文基线和各语言覆盖文件？

阶段验收：

- 在不加入任何中文文本时，英文行为与原 `bleed` 一致。
- 模拟一个最小测试语言包，可以切换、重启并回退。
- 删除测试语言包后，旧配置不会导致启动失败。
- `./make.ps1 test`、`./make.ps1 tests` 通过。

### 阶段 2：字体方案

不要直接接受 PR 中的字体文件，先完成许可证和覆盖范围审查。

要求：

1. 字体许可证允许源码仓库和二进制发行包再分发。
2. 保存字体许可证文本、来源 URL、版本和校验值。
3. 覆盖简体中文实际用字、ASCII、数字和常用标点。
4. Regular、Bold、Small、Tiny 等 UI 字体角色显示一致；不能只有某一字号支持中文。
5. 评估字形缓存、纹理图集大小、启动时间和内存变化。
6. 验证中英文混排、玩家名、服务器名、快捷键、数字和百分号。

建议优先评估成熟的开源 CJK 字体族，但最终选型必须依据许可证、文件大小、渲染效果和 OpenRA 字体管线实测决定。

阶段验收：

- 扫描所有中文 `.ftl` 后，不存在缺失字形。
- 主菜单、设置、多人大厅、单位提示、战役目标没有方框字或乱码。
- 字体版权信息进入仓库的第三方许可记录。

### 阶段 3：导入中文初始语料

只提取 #22531 中与中文直接相关的变更，重点参考 `44f26e5` 和 `96c5ebf`。不得顺带导入 `7dd4b49` 的俄语资源。

导入规则：

1. 保留英文文件作为 canonical source；中文文件只做覆盖。
2. Fluent message ID、attribute 名和变量名不得翻译。
3. 保留 `$variable`、选择表达式、占位符和必要的格式控制。
4. 对当前 `bleed` 已删除的英文 key，不应机械恢复旧 key。
5. 对当前 `bleed` 新增的英文 key，先以英文回退并进入待翻译清单。
6. 如果英文 key 的变量集合已变化，中文条目必须同步结构后才能标记完成。
7. 导入提交只做机械迁移和冲突适配，不在同一提交内大规模润色，以便审查来源差异。

阶段验收：

- 集成分支不包含 `fluent/ru/` 或俄语配置。
- 中文资源能被全部解析。
- 所有中文条目的变量集合与对应英文条目一致。
- 缺失中文条目可回退英文，不显示裸 key。

### 阶段 4：建立术语表与文风规范

新增一份版本化术语表，例如 `docs/translation/zh-CN-glossary.csv` 或 `.md`。术语表至少包含：

- 英文原文；
- 推荐中文；
- 禁用或不推荐译法；
- 所属作品：Tiberian Dawn、Red Alert、Dune 2000、Tiberian Sun；
- 上下文和备注；
- 参考来源；
- 审核状态和审核人。

翻译优先级：

1. 中国玩家社区已有稳定译名；
2. 有可靠依据的官方中文译名；
3. 保留英文或音译；
4. 新造译名必须记录理由，不能在不同文件中各自发挥。

文风要求：

- UI 按钮简短、可操作，例如 “应用”“返回”“断开连接”。
- 单位和建筑名称保持跨界面、百科、生产栏和任务文本一致。
- 游戏规则说明优先准确，不为追求文采改变数值或条件含义。
- 战役对白可以自然化，但不得改变任务目标、阵营关系和触发条件。
- 谨慎处理 Commander、Credits、Power、Ore、Spice、Tiberium、Shroud、Fog of War 等高频术语。
- 标点默认使用中文全角标点；代码、快捷键、路径、数值单位和变量周围按实际 UI 需要处理。
- 避免无意义的英文直译、网络流行语和不同年代作品之间术语串用。

### 阶段 5：分模块人工校订

建议顺序：

1. `mods/common` 和 `mods/common-content`
2. 设置、主菜单、服务器浏览器、多人大厅和地图编辑器公共界面
3. CNC 单位、建筑、规则和战役
4. RA 单位、建筑、规则和战役
5. D2K 单位、建筑、规则和战役
6. TS 单位、建筑、规则和已有战役内容
7. 地图专属 `map.ftl`、调试文本和低频工具界面

每个模块执行两轮审查：

- 语义审查：是否准确反映英文、变量和游戏机制。
- 游戏内审查：是否溢出、歧义、断行错误或脱离上下文。

Agent 可以生成候选译文和一致性报告，但以下内容必须由人工或独立审校 Agent 复核：

- 单位、建筑、武器、阵营和角色专名；
- 胜负条件、任务目标和提示；
- 包含数值、方向、时间、资源和条件分支的文本；
- PR 原作者已标记需要人工复核的 LLM 翻译。

### 阶段 6：自动化质量门禁

在现有 lint 之外增加中文专用检查。建议工具读取 Fluent AST，禁止用脆弱的正则表达式完整解析 Fluent。

至少检查：

1. 中文相对英文的缺失 key、额外 key和重复 key。
2. 每个 key 的 attribute 集合一致性。
3. 每个 message/attribute 使用的变量集合一致性。
4. Fluent 语法错误和 Junk 节点。
5. 中文值为空或只有空白。
6. 疑似未翻译的长英文句子；白名单允许专名、协议名和快捷键。
7. 禁用术语和同一英文术语的非预期多译。
8. 中文使用字符是否全部被选定字体覆盖。
9. 中文文件是否仍对应当前英文源文件。
10. 翻译覆盖率报告，按 Mod 和文件分类，而不是只给一个总百分比。

建议将报告分为：

- Error：Fluent 无法解析、变量不匹配、字体缺字、运行时会显示裸 key。
- Warning：缺失翻译、疑似英文残留、术语不一致、可能的过长文本。
- Info：覆盖率、新增英文 key、待人工复核条目。

阶段验收：

```powershell
./make.ps1 check
./make.ps1 test
./make.ps1 tests
```

以上命令必须成功；若某个命令因环境或已有上游问题失败，Agent 必须记录精确命令、失败阶段和原始错误，不能只写“测试失败”。

### 阶段 7：运行时验收

至少覆盖以下场景：

| 场景 | 验收重点 |
| --- | --- |
| 首次启动 | 默认英文或明确的系统语言策略；不能因系统为中文而崩溃 |
| 语言切换 | 选择中文、保存、重启、仍为中文 |
| 回退 | 缺少中文 key 时显示英文，不显示 key |
| 主菜单 | 按钮无截断、重叠和乱码 |
| 全部设置页 | 下拉框、滑块、说明、快捷键文本正常 |
| 单人游戏 | 地图选择、难度、游戏选项正常 |
| 多人大厅 | 玩家名、地图名、服务器消息和系统提示正常 |
| CNC/RA/D2K/TS | 各启动一次遭遇战，检查生产栏、工具提示、资源与胜负信息 |
| 战役 | 每个作品至少检查开场简报、任务目标、目标更新、胜利和失败 |
| 地图编辑器 | 菜单、工具提示、属性和保存提示正常 |
| 不支持字符 | 玩家名或服务器名包含中文时不崩溃、不出现方框字 |
| 切回英文 | 英文资源完全恢复，无中文残留 |

对固定窗口尺寸和常见 UI 缩放比例保存截图证据。发现溢出时优先优化译文；只有无法合理缩短时才做小范围、可解释的布局调整。

## 6. Agent 工作规则

后续执行本计划的 Agent 必须遵守：

1. 开始前读取仓库贡献指南和任何 `AGENTS.md`。
2. 每批工作前记录基准 SHA、目标模块和允许修改的文件集合。
3. 不直接把 #22531 整体 merge 到长期分支。
4. 不引入俄语翻译，除非用户以后明确要求多语言包。
5. 不在翻译任务中顺手修改游戏平衡或战役逻辑。
6. 不改 Fluent key、attribute、变量名，除非属于单独评审的上游代码修复。
7. 不把 LLM 输出直接视为已审核翻译。
8. 对不确定专名写入术语表的待定区，不能静默猜测。
9. 保留原贡献者署名和提交来源。
10. 每次提交只覆盖一个清晰模块或一种工程目的。
11. 运行与改动风险相称的验证；不得只凭文件存在宣称功能完成。
12. 最终报告必须区分：已验证事实、翻译/架构判断、未验证事项。

## 7. 每批翻译任务模板

可将以下模板直接交给执行 Agent：

```text
目标：校订 OpenRA 简体中文的 <模块名称>。

基准：当前 zh-cn 集成分支；先记录 HEAD 和对应 upstream/bleed SHA。
范围：仅修改 <允许的目录或文件>、术语表，以及必要的测试快照。
来源：英文 Fluent 文件为语义基准；#22531 中文文本仅作候选语料。

要求：
1. 不修改 Fluent ID、attribute 名、变量名及游戏逻辑。
2. 对照实际调用上下文和游戏机制，不逐句盲译。
3. 优先使用术语表；遇到未决专名先登记再决定。
4. 检查英文与中文的 key、attribute、变量集合。
5. 运行中文校验工具、./make.ps1 test；涉及代码时再运行 ./make.ps1 check 和 ./make.ps1 tests。
6. 在游戏内检查本模块的关键界面，并记录窗口大小、操作路径和结果。
7. 单独列出仍需人工判断的条目，不得把不确定项标记为完成。

交付：
- 修改文件列表；
- 术语表新增/变更；
- 覆盖率与剩余英文报告；
- 精确测试命令及结果；
- 游戏内验收证据；
- 未解决问题。
```

## 8. 完成定义

只有同时满足以下条件，才能称为“可发布简体中文版”：

- 多语言框架与中文内容已经拆分，分支历史可审查。
- 所有官方 Mod 均可启动，中文切换和英文回退有效。
- Fluent 语法、key、attribute 和变量一致性检查全部通过。
- 中文字符字体覆盖检查通过，字体许可证完整。
- `./make.ps1 check`、`./make.ps1 test`、`./make.ps1 tests` 通过，或仅剩有完整证据证明与本分支无关的已知上游失败。
- 公共界面及高风险游戏文本完成独立复核。
- CNC、RA、D2K、TS 都完成规定的运行时冒烟测试。
- 不存在会阻止操作的严重文本溢出、乱码或裸 Fluent key。
- 术语表、覆盖率报告、来源记录和未完成清单随版本一起发布。

“所有文件都包含中文”不等于完成；“lint 通过”也不等于译文准确。工程正确性、语言质量和游戏内可用性必须分别验收。

## 9. 向上游贡献的推荐拆分

如果目标包括回馈 OpenRA 上游，建议依次提交：

1. 通用多语言框架 PR：不含具体俄语或中文大包，重点证明回退、安全边界、配置兼容和测试。
2. CJK 字体支持 PR：附许可证、来源、大小及性能说明；也可在上游认可时与框架 PR 合并。
3. 简体中文资源 PR：按公共 UI 和各 Mod 分批，附术语表、覆盖率、人工复核范围和截图。
4. 后续小 PR：持续同步英文新增 key、修正术语和布局。

在开始大规模重复翻译前，应先在 #22531 留言联系 Seaear，说明计划复用并校订其贡献，询问其是否愿意协作或接受拆分。这样可以保留作者权益，也能减少平行分支重复劳动。

## 10. 第一轮执行建议

第一位接手 Agent 应只完成以下有限任务，不要立即开始批量润色 7300 行文本：

1. 获取并归档 #22531 最新 head，生成与当前 `bleed` 的文件和提交清单。
2. 把框架、俄语、中文、字体四类改动准确分类。
3. 输出语言框架的最小移植设计和受影响文件列表。
4. 建立 `feature/localization-framework`，确保纯英文行为不变并通过测试。
5. 审计 PR 字体的名称、来源、许可证、字符覆盖和文件大小。
6. 建立中文术语表骨架和自动一致性检查器。
7. 之后才将中文资源机械导入独立提交。

这样能先把最容易导致返工的架构、字体和来源问题解决，再投入成本最高的人工校订。
